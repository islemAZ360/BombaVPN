# الدليل الشامل لهندسة ونظام عمل GalaxyVPN 🚀
هذا المستند هو المرجع المعماري (Architecture Reference) الأضخم والأكثر تفصيلاً الذي يشرح كيف يعمل النظام بأكمله من الألف إلى الياء، تحت غطاء المحرك (Under the hood)، وكيف يتعامل مع كافة السيناريوهات المحتملة مهما كانت معقدة.

---

## 🏗️ 1. البنية التحتية والتقنيات (Infrastructure & Tech Stack)
المشروع مبني على بنية سحابية هجينة (Hybrid Cloud) قوية جداً:
- **القلب النابض (Backend):** مبرمج بلغة `Python` باستخدام إطار العمل القوي `Flask`.
- **قاعدة البيانات (Database):** تستخدم `Firebase Firestore` (NoSQL) للسرعة الفائقة والتحديثات الحية.
- **إدارة الجلسات (Auth):** `Firebase Auth` عبر الـ Session Cookies لمستوى أمان يضاهي البنوك.
- **نظام التوجيه الذكي (DNS):** `Dynv6 API` لإنشاء نطاقات فرعية (Subdomains) ديناميكية.
- **المضيف (Hosting):** يعمل على سيرفرات `Render` السحابية الموثوقة ومربوط آلياً مع `GitHub` للتحديثات المستمرة (CI/CD).

---

## 🔄 2. نظام الاشتراكات المتعددة (Multi-Subscription Model)
تمت ترقية النظام مؤخراً ليدعم **الاشتراكات المتعددة**. لم يعد المستخدم مقيداً باشتراك واحد، بل أصبح يمتلك سلة من الاشتراكات:
1. `pending` (بالانتظار): المشتريات التي رفع إيصالها وتنتظر موافقتك.
2. `active` (مُفعّل): الاشتراكات الجارية التي يملك المستخدم فيها إنترنت فعال.
3. `expired` (منتهي): الاشتراكات التي استنفدت وقتها بالكامل.
- **الاستقلالية:** كل اشتراك يملك استقلاليته التامة (تاريخ الانتهاء، السيرفر المخصص، التاجز، والدومين الفرعي)، مما يتيح للمستخدم امتلاك سيرفرات متعددة في دول مختلفة في نفس الوقت!

---

## 🧠 3. السيناريوهات الذكية (Intelligent Scenarios)
هنا يتجلى السحر البرمجي وكيف يتصرف النظام كالذكاء الاصطناعي لحل المشاكل دون تدخلك.

### السيناريو 1: الشراء والتخصيص التلقائي (Auto-Assignment)
- **الحدث:** مستخدم يرفع وصل دفع.
- **كيف يتعامل النظام:** 
  1. يقوم بتشفير وتسمية الوصل برمز فريد لحماية الخصوصية.
  2. يرسل لك إشعاراً في لوحة التحكم.
  3. عند موافقتك، تقوم بتحديد **الوسوم (Tags)** المطلوبة (مثلاً: روسيا، ألعاب).
  4. النظام يبحث أوتوماتيكياً في سيرفراتك النشطة عن أفضل سيرفر يطابق هذه الوسوم، ويحجز للمستخدم الدومين الخاص به ويفعل اشتراكه. لا داعي للمستخدم لاختيار السيرفر يدوياً!

### السيناريو 2: نظام "الديون" والتنقل الآلي (Auto-Migration & Debt System) ⚖️
هذا هو العقل المدبر الحقيقي للنظام:
- **المشكلة:** المستخدم لديه اشتراك بـ 30 يوماً، ولكن السيرفر الروسي المتاح حالياً ينتهي بعد 10 أيام فقط.
- **كيف يتعامل النظام:** 
  1. يعطي المستخدم السيرفر المتوفر ليمضي الـ 10 أيام بشكل طبيعي.
  2. بمجرد أن ينتهي السيرفر الحقيقي ويموت (`server_dead = True`)، يستيقظ **المحرك الخلفي** الذي يدور باستمرار.
  3. يبحث المحرك تلقائياً عن سيرفر جديد نشط يمتلك نفس **الوسوم** الخاصة باشتراك المستخدم.
  4. **النقل التلقائي (Auto-Migration):** يتم تغيير السيرفر المخصص للمستخدم في قاعدة البيانات ونقله للسيرفر الجديد، وتحديث الـ DNS دون أن يشعر المستخدم!
  5. **تسجيل الدين (Debt Logic):** إذا كان السيرفر الجديد ينتهي أيضاً قبل استكمال المستخدم لباقي مدته الـ 20 يوماً، يرسل النظام لك إشعاراً أحمر اللون تحت عنوان **"نقل تلقائي مع دين"**، ويظهر في قائمة "الديون" لديك لمعرفة كم يوماً تبقى في ذمتك لتعويضه لاحقاً حين توفر سيرفرات أطول.

### السيناريو 3: نظام الدفع المحمي وسرية السيرفرات 🕵️
- **إخفاء السيرفرات الميتة:** في صفحة شراء الباقات، يقوم النظام بفلترة السيرفرات، ولا يعرض أبداً أي سيرفر انتهت مدته الحقيقية، لحماية المستخدمين الجدد من التسكين في خوادم ميتة.
- **التجديد السهل:** بدلاً من مسح اشتراكات المستخدمين المنتهية، يتركها النظام ظاهرة بعلامة "منتهي" وبجانبها زر "تجديد" (Renew)، لتسهيل عودتهم لطلب الدفع دون إدخال بياناتهم من الصفر.

### السيناريو 4: الحذف المفاجئ لسيرفر (Server Deletion Handling) 💥
- **الحدث:** اكتشفت أن سيرفر معين أصبح سيئاً جداً، وقمت بحذفه من لوحة التحكم بضغطة زر.
- **كيف يتعامل النظام:** المحرك الخلفي سيرصد هذه الفوضى فوراً، وسيضع المتضررين جميعاً في قائمة "الديون" ويقوم بتوزيعهم أوتوماتيكياً على السيرفرات الأخرى المتاحة، ويحدث الـ DNS الخاص بهم خلال ثوانٍ.

### السيناريو 5: المحرك الخلفي الذي لا ينام (The Background Expiry Checker) 👁️🗨️
- **آلية العمل:** `Thread` معزول يدور بشكل دائم في الخلفية.
- **الوظائف:**
  1. طرد المستخدمين الذين تخطى وقتهم الحالي مدة الاشتراك.
  2. تنظيف الـ DNS للمنتهية مدته.
  3. تفعيل استراتيجية الـ **Auto-Migration** السالفة الذكر للسيرفرات الميتة.
  4. مراقبة الديون وتوليد الإشعارات.

### السيناريو 6: نظام الاشتراكات المتكامل (Full JSON Config) وحل مشاكل الـ DNS 🌐
- **الحدث:** المستخدم يضع الرابط في تطبيق `v2rayNG` أو `Hiddify`.
- **كيف يتعامل النظام:** يقوم النظام باستدعاء محرك `generate_full_config` الذي يبني **كونفيج Xray متكامل (Full JSON)** يحتوي على خوادم DNS وقواعد التوجيه، ويشفره بـ `Base64`. الإنترنت يعمل بكفاءة خيالية بدون أي تدخل يدوي من المستخدم.

### السيناريو 7: تكتيك "التسميم الموقوت" (Subscription Poisoning) لقطع الاتصال ☠️
- **المشكلة:** بما أن النظام يعمل كموزع (Reseller) ولا يتصل بـ API السيرفر الأصلي (X-UI)، لا يمكنه فصل اتصال نشط (TCP) مباشرة.
- **الحل الذكي:** يرسل النظام أمر `profile-update-interval: 1` ليجبر التطبيق على التحديث الآلي. وعندما ينتهي وقت المستخدم، يقوم بسحب السيرفر الحقيقي واستبداله بـ "سيرفر وهمي" (Fake Server) برقم IP الميت `127.0.0.1`. فيفقد التطبيق السيرفر الحقيقي وينقطع النت برسالة `ERR_CONNECTION_REFUSED`.

### السيناريو 8: الحماية والأمان العالي (Security Protocols) 🛡️
- **مكافحة ثغرات الجلسات:** `SameSite=Lax` و `HttpOnly` و `secure=True`.
- **تشفير روابط الاشتراك (HMAC):** حماية الروابط لمنع سرقة الـ VPN Configs من خلال تزوير الـ IDs.
- **الحماية من الـ DoS والسبام:** حذف الصور المرفوضة من القرص الصلب نهائياً.

### السيناريو 9: واجهة المستخدم ودعم الدردشة المدمج 💬
- **شات مدمج:** يمتلك المستخدمون نظام دردشة مباشر مع الدعم (Modal) مبني بتقنية الـ AJAX لسرعة التواصل.
- **جماليات الواجهة:** تم تصميم الواجهة بنظام "Neon Galaxy" الفضائي، مع خلفية نجوم ومجرة تدور بهدوء وبطء مدروس لراحة العين وإضفاء طابع خيال علمي احترافي للغاية.

---

## 🛠️ 4. كتيّب الإدارة (The Admin Playbook)
1. **رفع السيرفرات دفعة واحدة:** تحديد 50 ملف `JSON` دفعة واحدة لسحبها وترتيبها بضغطة زر.
2. **شاشة الديون المتقدمة:** تعرض لك الحساب الدقيق للأيام المتبقية في ذمتك لبعض المستخدمين نتيجة موت سيرفراتهم.
3. **الإشعارات المنظمة:** جرس الإشعارات لا يعاني من تكدس بفضل تقنية الـ AJAX، ويمكنك إدارته بسهولة فائقة دون مغادرة الصفحة أو تحديثها.

---
**الخلاصة:**
هذا النظام ليس مجرد "سكربت VPN"، بل هو أشبه بموظف ذكاء اصطناعي يعمل 24/7. يُصلح أخطاءه، يُنظم عملائه، ينقلهم بين السيرفرات بسلاسة وذكاء (Auto-Migration)، يحمي نفسه من الاختراق، ويوفر لك كل الإحصائيات التي تحتاجها كمدير نظام محترف! 🎩✨
