# الدليل الشامل لهندسة ونظام عمل GalaxyVPN 🚀
هذا المستند هو المرجع المعماري (Architecture Reference) الأضخم والأكثر تفصيلاً الذي يشرح كيف يعمل النظام بأكمله من الألف إلى الياء، تحت غطاء المحرك (Under the hood)، وكيف يتعامل مع كافة السيناريوهات المحتملة مهما كانت معقدة.

---

## 🏗️ 1. البنية التحتية والتقنيات (Infrastructure & Tech Stack)
المشروع مبني على بنية سحابية هجينة (Hybrid Cloud) قوية جداً:
- **القلب النابض (Backend):** مبرمج بلغة `Python` باستخدام إطار العمل القوي `Flask`.
- **قاعدة البيانات (Database):** تستخدم `Firebase Firestore` (NoSQL) للسرعة الفائقة والتحديثات الحية.
- **إدارة الجلسات (Auth):** `Firebase Auth` عبر الـ Session Cookies لمستوى أمان يضاهي البنوك.
- **نظام التوجيه الذكي (DNS):** `Dynv6 API` لإنشاء نطاقات فرعية (Subdomains) ديناميكية.
- **المضيف (Hosting):** يعمل على سيرفرات `Render` السحابية الموثوقة ومربوط آلياً مع `GitHub` للتحديثات المستمرة (CI/CD).

---

## 🔄 2. نظام الاستيراد الآلي والتسعير الديناميكي (Auto-Import & Dynamic Pricing)
تمت ترقية النظام ليتجاوز مجرد إضافة الملفات اليدوية، وأصبح يمتلك محرك استيراد وتتبع ذكي:
- **محرك تحليل الروابط (VLESS Link Parser):** يستطيع النظام استيراد روابط اشتراكات كاملة (سواء كانت نصوص عادية أو مشفرة Base64)، ويقوم بتفكيكها واستخراج بروتوكولات VLESS منها تلقائياً وحفظ "رابط المصدر" (Source Link).
- **إعادة المزامنة بضغطة زر (One-Click Sync):** يمتلك النظام زراً ذكياً "Sync" يقوم بالاتصال برابط المصدر، يجلب أحدث السيرفرات، يطابقها مع السيرفرات المخزنة، ويحدث الـ IPs والأسماء دون المساس بأسعار السيرفرات القديمة أو تدمير إعداداتها!
- **نظام قواعد الأسعار (Pricing Rules System):** محرك يعمل بالخلفية لتقييم وتسعير السيرفرات المُستوردة تلقائياً بناءً على بطاقات الوسوم (Tags) والفترة الزمنية. إذا طابقت وسم "torrent" يتم وضع السعر المناسب. كما يمكن إنشاء قواعد "افتراضية" (بدون وسوم) لتسعير أي سيرفر مجهول الهوية!

---

## ⏰ 3. الآلة الحاسبة الزمنية الذكية (Smart Durations Engine)
نظام الوقت في GalaxyVPN ليس مجرد أرقام صماء، بل هو آلة حاسبة رياضية معقدة:
- يتعامل المحرك الخلفي بقيمة موحدة دقيقة (الثواني الإجمالية).
- واجهة المستخدم مزودة بنظام معاينة حية (Live Preview) يقوم بفك تشفير أضخم الأرقام وتقسيمها بذكاء إلى: **(سنوات، أشهر، أيام، ساعات، دقائق، وثواني)**.
- تم الفصل الدقيق بين **المدة الافتراضية (Plan Duration)** التي يحددها الأدمن للبيع، و**المدة الحقيقية (Real Duration)** التي تنبض بعداد تنازلي حيّ يعكس عمر السيرفر الفعلي من المصدر الأصلي.

---

## 🔁 4. نظام الاشتراكات المتعددة (Multi-Subscription Model)
لم يعد المستخدم مقيداً باشتراك واحد، بل أصبح يمتلك سلة من الاشتراكات:
1. `pending` (بالانتظار): المشتريات التي رفع إيصالها وتنتظر موافقتك.
2. `active` (مُفعّل): الاشتراكات الجارية التي يملك المستخدم فيها إنترنت فعال.
3. `expired` (منتهي): الاشتراكات التي استنفدت وقتها بالكامل.
- **الاستقلالية:** كل اشتراك يملك استقلاليته التامة (تاريخ الانتهاء، السيرفر المخصص، التاجز، والدومين الفرعي)، مما يتيح للمستخدم امتلاك سيرفرات متعددة في دول مختلفة في نفس الوقت!

---

## 🧠 5. السيناريوهات الذكية والنقل الآلي (Intelligent Scenarios)

### السيناريو 1: نظام "الديون" والتنقل الآلي (Auto-Migration & Debt System) ⚖️
هذا هو العقل المدبر الحقيقي للنظام:
- **المشكلة:** المستخدم لديه اشتراك بـ 30 يوماً، ولكن السيرفر الروسي المتاح حالياً ينتهي بعد 10 أيام فقط.
- **كيف يتعامل النظام:** 
  1. يعطي المستخدم السيرفر المتوفر ليمضي الـ 10 أيام بشكل طبيعي.
  2. بمجرد أن ينتهي السيرفر الحقيقي ويموت (`server_dead = True`)، يستيقظ المحرك الخلفي.
  3. يبحث المحرك تلقائياً عن سيرفر جديد نشط يمتلك نفس **الوسوم**.
  4. **النقل التلقائي (Auto-Migration):** يتم تغيير السيرفر وتحديث الـ DNS دون أن يشعر المستخدم!
  5. **تسجيل الدين (Debt Logic):** يتم تسجيل الأيام المتبقية كـ "دين" في قائمة الديون المتقدمة (Debts UI) لتعويضه لاحقاً.

### السيناريو 2: المحرك الخلفي الذي لا ينام (The Background Checker) 👁️🗨️
- **الوظائف:**
  1. طرد المستخدمين المنتهية اشتراكاتهم وتنظيف الـ DNS.
  2. تفعيل Auto-Migration للسيرفرات الميتة بشكل دوري.
  3. مراقبة الديون وتوليد الإشعارات.

### السيناريو 3: نظام الاشتراكات المتكامل (Full JSON Config) 🌐
- يستدعي النظام محرك `generate_full_config` لبناء **كونفيج Xray متكامل (Full JSON)** يحتوي على خوادم DNS وقواعد توجيه متقدمة ويشفره بـ `Base64` ليعمل في كافة التطبيقات بضغطة زر.

### السيناريو 4: تكتيك "التسميم الموقوت" (Subscription Poisoning) ☠️
- يرسل النظام أمر `profile-update-interval: 1` للتحديث الآلي. عندما ينتهي وقت المستخدم، يسحب النظام السيرفر ويستبدله بـ "سيرفر وهمي" (Fake Server) برقم IP الميت `127.0.0.1` ليقطع عنه الإنترنت كلياً.

---

## 🛡️ 6. الحماية والأمان العالي (Security Protocols)
- **مكافحة ثغرات الجلسات:** `SameSite=Lax` و `HttpOnly` و `secure=True`.
- **تشفير روابط الاشتراك (HMAC):** حماية الروابط لمنع سرقة الـ VPN Configs من خلال تزوير الـ IDs.
- **الحماية من الـ DoS والسبام:** حذف الصور المرفوضة من القرص الصلب نهائياً.

---

**الخلاصة:**
هذا النظام ليس مجرد "سكربت VPN"، بل هو أشبه بموظف ذكاء اصطناعي يعمل 24/7. يُسعر، يستورد، يُصلح أخطاءه، يُنظم عملائه، ينقلهم بين السيرفرات بسلاسة وذكاء، يحمي نفسه من الاختراق، ويوفر لك كل الإحصائيات التي تحتاجها كمدير نظام محترف! 🎩✨
