# الدليل الشامل لهندسة ونظام عمل GalaxyVPN 🚀

هذا المستند هو المرجع المعماري (Architecture Reference) الذي يشرح كيف يعمل النظام بأكمله من الألف إلى الياء، تحت غطاء المحرك (Under the hood). يعكس هذا المستند الحالة الفعلية للكود بعد الهجرة إلى Supabase وكل التحديثات الأخيرة.

---

## 🏗️ 1. البنية التحتية والتقنيات (Infrastructure & Tech Stack)

- **القلب النابض (Backend):** `Python` + `Flask`، مقسّم إلى Blueprints منظّمة:
  - `routes/auth_routes.py` (تسجيل الدخول/الجلسات)، `routes/main_routes.py` (المستخدم/الدفع/الدعم)، `routes/admin_routes.py` (لوحة الأدمن)، `routes/api_routes.py` (واجهات AJAX + webhook تيليجرام).
- **قاعدة البيانات (Database):** `Supabase` (PostgreSQL) عبر مكتبة `supabase-py` (REST/PostgREST). الجداول الأساسية: `users`, `servers`, `subscriptions`, `purchase_requests`, `messages`, `admin_messages`, `notifications`, `pricing_rules`, `source_links`, `debts`.
  - ⚠️ ملاحظة مهمة: تعديل المخطط (إضافة أعمدة/جداول) **يدوي** عبر Supabase SQL Editor — لا يملك الكود وصولاً لتنفيذ DDL. ملفات الهجرة موجودة في مجلد `migrations/`.
- **إدارة الجلسات (Auth):** `Supabase Auth` — يُخزَّن `access_token` في كوكي `session` (HttpOnly، secure في الإنتاج، SameSite=Lax).
- **التخزين المؤقت (Caching):** `db_helpers.py` يستخدم `TTLCache` بمدة 60 ثانية لقراءات `get_all_*`. بعد أي كتابة تُستدعى `invalidate_*` لتفريغ الكاش فوراً حتى تظهر التغييرات بلا تأخير.
- **المضيف (Hosting):** `Render` السحابي، مربوط آلياً مع `GitHub` (CI/CD): كل `git push` على الفرع `main` يُطلق إعادة نشر تلقائية.

---

## 🔄 2. الاستيراد الآلي والتسعير الديناميكي (Auto-Import & Dynamic Pricing)

- **محرك تحليل VLESS (`vless_parser.py`):** يستورد روابط اشتراك كاملة (نص عادي أو Base64)، يفك تشفيرها، ويستخرج بروتوكولات `vless://` ويحوّلها إلى كونفيج Xray، مع حفظ "رابط المصدر" في `source_links`.
- **منطق الاستيراد المشترك (`helpers._import_vless_servers`):** يُستخدم في الاستيراد اليدوي وفي مزامنة الروابط (`Sync`). يحسب الدولة من الـ IP، يبني اسماً منظّماً، ويُسعّر السيرفر بقواعد التسعير، ثم يُدخله في جدول `servers`. السعر يُحوَّل بأمان (`float`) فإن كان فارغاً يصبح `0.0` بدل أن ينهار الاستيراد.
- **المزامنة بضغطة زر (Sync):** `/api/admin/sync_link/<id>` يعيد جلب الرابط ويضيف الجديد فقط (`dedup`) دون تكرار أو تدمير السيرفرات القديمة.
- **نظام قواعد التسعير (Pricing Rules):**
  - المطابقة تعتمد على **الوسوم + المدة معاً**: تُختار أكثر قاعدة تخصصاً (أكبر عدد وسوم مطابقة) بشرط أن تكون وسوم القاعدة ⊆ وسوم السيرفر **و** أن تساوي `total_duration_seconds` للقاعدة مدة خطة السيرفر بالضبط.
  - **تطبيق تلقائي رجعي:** عند إضافة/تعديل/حذف أي قاعدة تُستدعى `helpers.reprice_all_servers()` لإعادة تسعير كل السيرفرات فوراً (لا حاجة لـ Re-Scan يدوي). زر **Re-Scan** متاح أيضاً.
  - **تعديل مباشر (Inline Edit):** كل قاعدة فيها زر تعديل يحوّلها لنموذج تحرير (وسوم/مدة/سعر) عبر `POST /api/admin/pricing_rules/<id>/update` دون حذف وإعادة إنشاء.

---

## ⏰ 3. الآلة الحاسبة الزمنية الذكية (Smart Durations Engine)

- المحرك الخلفي يخزّن قيمة موحدة دقيقة (`total_plan_seconds` / `total_duration_seconds`).
- الواجهة فيها معاينة حية (Live Preview) تفكّ الأرقام إلى **(سنوات، أشهر، أيام، ساعات، دقائق، ثواني)**، وتعرض المدة المحفوظة بشكل مختصر (تتجاهل الأجزاء الصفرية) بدل عرض حقل الأيام الخام فقط.
- فصل دقيق بين **المدة المعروضة للمستخدم (Plan Duration)** و**المدة الحقيقية للسيرفر (Real Duration)** ذات العداد التنازلي الحي.

---

## 🔁 4. نموذج الاشتراكات المتعددة (Multi-Subscription Model)

كل مستخدم يملك سلّة اشتراكات مستقلة في جدول `subscriptions`:
1. `pending`: طلب رُفع إيصاله وينتظر الموافقة (في جدول `purchase_requests`).
2. `active`: اشتراك جارٍ بإنترنت فعّال.
3. `expired`: اشتراك استنفد وقته.

كل اشتراك مستقل (تاريخ انتهاء، سيرفر مخصص، وسوم) فيتيح امتلاك سيرفرات متعددة بدول مختلفة في آنٍ واحد.

---

## 💳 5. دورة الدفع والموافقة (Payment → Approval Lifecycle)

1. **المستخدم** يختار سيرفراً ويرفع إيصال الدفع في `/pay` → يُنشأ صف في `purchase_requests` (مع `email`, `price`, `server_id`).
2. **إشعار تيليجرام فوري:** يرسل الخادم صورة الإيصال إلى الأدمن مع زرّي **Approve / Reject** (مستقل تماماً عن فتح لوحة الأدمن).
3. **الموافقة** (من اللوحة أو من تيليجرام) تستدعي `helpers._approve_request_logic`: يُنشأ اشتراك `active`، يُحدَّث حالة المستخدم، ويُطبَّق نظام الإحالة.
4. **التعيين اليدوي (Assign Server):** يستطيع الأدمن إسناد سيرفر لمستخدم مباشرة عبر `/admin/manage_user_sub/<user_id>` (action=assign) فيُنشأ اشتراك نشط بمدة السيرفر.

### 🤖 تكامل تيليجرام (Telegram Bot)
- الإرسال يحتاج `TELEGRAM_BOT_TOKEN` و`TELEGRAM_CHAT_ID` في بيئة Render.
- أزرار القبول/الرفض تعمل عبر `POST /api/telegram/webhook`. **يُسجَّل الـ webhook تلقائياً** عند أول طلب بعد كل نشر (باستخدام `RENDER_EXTERNAL_URL`)، فلا حاجة لأي إعداد يدوي.

### 🎁 نظام الإحالة (Referral Reward)
- عند تسجيل مستخدم عبر رابط إحالة يُحفظ `referred_by`. وعند الموافقة على **أول** عملية شراء له:
  - المُحال يحصل على **+7 أيام** على اشتراكه الجديد.
  - صاحب الرابط يحصل على **+7 أيام** على أحدث اشتراك نشط (أو يُعاد تفعيل منتهٍ) — بشرط أن يكون قد اشترى اشتراكاً واحداً على الأقل.
  - يُمسح `referred_by` بعدها (المكافأة لمرة واحدة).

---

## 🧠 6. السيناريوهات الذكية والنقل الآلي (Intelligent Scenarios)

### نظام "الديون" والنقل الآلي (Auto-Migration & Debt) ⚖️
- إن أُعطي المستخدم سيرفراً ينتهي قبل اشتراكه، يستيقظ المحرك الخلفي عند موت السيرفر، يبحث عن سيرفر نشط بنفس الوسوم، يُنقل المستخدم تلقائياً، ويُسجّل الفارق كـ "دين" في جدول `debts` مع إشعار.

### المحرك الخلفي (`worker.py`) الذي لا ينام 👁️
1. طرد المنتهية اشتراكاتهم وتنظيف التخصيص.
2. النقل الذكي ذو الأولويات للسيرفرات الميتة بشكل دوري.
3. مراقبة الديون وتوليد الإشعارات في جدول `notifications`.

### الكونفيج الكامل (Full JSON Config) 🌐
- `generate_full_config` يبني كونفيج Xray متكاملاً (DNS + قواعد توجيه) ويشفّره Base64 ليعمل في كل التطبيقات.

### "التسميم الموقوت" (Subscription Poisoning) ☠️
- عند انتهاء وقت المستخدم يُستبدل السيرفر بـ IP ميت `127.0.0.1` لقطع الإنترنت كلياً.

---

## 💬 7. الدعم والإشعارات (Support & Notifications)

- **الدعم (Support):** رسائل المستخدم → جدول `messages`، وردود الأدمن → جدول `admin_messages`. تُجمَّع كمحادثة موحّدة عبر `/admin/api/chat/<user_id>` (مرتّبة زمنياً)، مع إشعار تيليجرام لكل رسالة جديدة.
- **الإشعارات (Notifications):** جدول `notifications` (`title`, `message`, `type`, `is_read`)؛ جرس الأدمن يعرض عدد غير المقروء، وصفحة `/admin/notifications` لإدارتها.

---

## 🖥️ 8. لوحة الأدمن كتطبيق صفحة واحدة (SPA Admin Dashboard)

- لا إعادة تحميل كاملة عند الإضافة/الحذف. تُستخدم مزامنات JSON موجّهة:
  - `fetchDashboardSync` (كل 5 ثوانٍ): الإحصائيات، السيرفرات، المستخدمون، روابط المصدر، التحليلات.
  - `fetchPendingRequests` (كل 3 ثوانٍ)، `fetchPricingRules` (كل 10 ثوانٍ).
- **معالج النماذج العام (Universal AJAX handler):** يعترض كل POST، يعرض Toast، ويُحدّث الجزء المعني فقط بدل إعادة بناء الصفحة.
- **النوافذ المنبثقة (Modals):** بسبب وجود `backdrop-filter` على `.container` (الذي يكسر `position:fixed`)، تُنقل النوافذ إلى `<body>` عند التحميل لتظهر دائماً في منتصف الشاشة الحالية بلا حاجة للتمرير.
- **التحليلات (Analytics):** KPIs (إجمالي/شهري/معلّق/ديون) + رسوم بيانية (Chart.js) للإيرادات وأنواع السيرفرات، وصفحة `transactions` مخصصة لسجل عمليات القبول/الرفض.

---

## 🛡️ 9. الحماية والأمان (Security)

- كوكي الجلسة: `HttpOnly` + `secure` (إنتاج) + `SameSite=Lax`.
- حماية CSRF بسيطة عبر فحص Origin/Referer لطلبات POST (مع استثناء webhook تيليجرام).
- تشفير روابط الاشتراك (`HMAC` عبر `sub_serializer`) لمنع تزوير المعرّفات.
- فحص SSRF (`is_safe_url`) قبل جلب روابط الاستيراد لمنع استهداف الشبكات المحلية.
- حذف الإيصالات المرفوضة من القرص نهائياً.

---

**الخلاصة:** GalaxyVPN منصة متكاملة: تستورد وتُسعّر تلقائياً، تدير دورة دفع كاملة عبر الويب وتيليجرام، تنقل المستخدمين بين السيرفرات بذكاء، تدير الدعم والإشعارات والإحالات، وتعمل كتطبيق SPA سلس — كل ذلك فوق Supabase وRender. 🎩✨
