# الدليل الشامل لهندسة ونظام عمل GalaxyVPN 🚀
هذا المستند هو المرجع المعماري (Architecture Reference) الأضخم والأكثر تفصيلاً الذي يشرح كيف يعمل النظام بأكمله من الألف إلى الياء، تحت غطاء المحرك (Under the hood)، وكيف يتعامل مع كافة السيناريوهات المحتملة مهما كانت معقدة.

---

## 🏗️ 1. البنية التحتية والتقنيات (Infrastructure & Tech Stack)
المشروع مبني على بنية سحابية هجينة (Hybrid Cloud) قوية جداً:
- **القلب النابض (Backend):** مبرمج بلغة `Python` باستخدام إطار العمل القوي `Flask`.
- **قاعدة البيانات (Database):** تستخدم `Firebase Firestore` (NoSQL) للسرعة الفائقة والتحديثات الحية.
- **إدارة الجلسات (Auth):** `Firebase Auth` عبر الـ Session Cookies لمستوى أمان يضاهي البنوك.
- **نظام التوجيه الذكي (DNS):** `Dynv6 API` لإنشاء نطاقات فرعية (Subdomains) ديناميكية (مثل `islam-gmail-com.galaxyvpn.dynv6.net`).
- **المضيف (Hosting):** يعمل على سيرفرات `Render` السحابية الموثوقة ومربوط آلياً مع `GitHub` للتحديثات المستمرة (CI/CD).

---

## 🔄 2. دورة حياة المستخدم (The User Journey & State Machine)
يمر المستخدم بـ 4 حالات أساسية (States) لا يمكن تخطيها أو التلاعب بها:
1. `pending` (بالانتظار): المستخدم سجل حسابه للتو، لا يملك أي وصول، يرى صفحة الدفع والأسعار.
2. `review` (قيد المراجعة): قام برفع وصل الدفع. لا يمكنه رفع وصل آخر (حماية من الـ Spam)، وينتظر قرارك.
3. `active` (مُفعّل): وافقت عليه، تم منحه سيرفر، تم حجز دومين خاص به (Subdomain)، وتم توليد كود `JSON` جاهز للنسخ في تطبيق `v2rayNG`.
4. `expired` (منتهي): انتهى وقته. يفقد الدومين فوراً، ويعود لصفحة الدفع.

---

## 🧠 3. السيناريوهات الذكية (Intelligent Scenarios)
هنا يتجلى السحر البرمجي وكيف يتصرف النظام كالذكاء الاصطناعي لحل المشاكل دون تدخلك.

### السيناريو 1: شراء اشتراك جديد
- **الحدث:** مستخدم يرفع وصل الدفع لباقة "شهر واحد".
- **كيف يتعامل النظام:** 
  1. يقوم بتشفير وتسمية الوصل برمز فريد (لتجنب التضارب وحماية الخصوصية).
  2. يضعه في حالة `review`. يرسل لك إشعاراً في لوحة التحكم.
  3. عندما تضغط "تفعيل"، يبحث النظام عن أفضل سيرفر نشط، ينشئ دومين للمستخدم، ويربط الدومين بـ IP السيرفر. ثم يحسب تاريخ الانتهاء (تاريخ اليوم + شهر).

### السيناريو 2: "التجديد المبكر" (Early Renewal) 💎
- **الحدث:** مستخدم `active` يتبقى له 5 أيام، ويريد التجديد الآن لكي لا ينقطع عنه الإنترنت.
- **كيف يتعامل النظام:**
  1. يسمح له النظام برفع وصل دفع جديد *بدون أن يقطع اتصاله* (يظل `active`).
  2. النظام يضع علامة `Renewal` (تجديد) خضراء لك في لوحة الإدارة.
  3. عندما توافق، يرى النظام أن لديه 5 أيام متبقية، فيقوم بجمعها بذكاء: (تاريخ الانتهاء الحالي + مدة الباقة الجديدة). فلا يضيع على المستخدم أي يوم أو ثانية!

### السيناريو 3: نظام "الديون" ونفاد مدة السيرفرات (Debt System) ⚖️
- **الحدث:** اشتريت سيرفر مدته 10 أيام. ومستخدم اشترى منك باقة شهر (30 يوم).
- **كيف يتعامل النظام:** 
  1. النظام يمنح المستخدم السيرفر المتوفر، ولكنه يسجل في قاعدة البيانات: "المستخدم له دين بقيمة 20 يوماً إضافياً".
  2. يتم إضافة المستخدم لقائمة "الديون" في لوحتك.
  3. **الاسترداد الآلي (Auto-Recovery):** المحرك الخلفي يفحص كل 60 ثانية. بمجرد أن تقوم أنت بضخ سيرفرات جديدة في النظام (مدتها طويلة)، سيقوم المحرك الخلفي باكتشافها فوراً، وسيقوم بنقل المستخدم صاحب الدين إلى السيرفر الجديد، ويطرد الدين! كل هذا دون أن يشعر المستخدم (لأن الدومين الثابت الخاص به سيتغير داخلياً للـ IP الجديد بسلاسة).

### السيناريو 4: كارثة نفاد السيرفرات (Temporary Servers Fallback) 🚨
- **الحدث:** كل سيرفراتك الروسية ممتلئة (أو انتهت)، ومستخدم يملك باقة يجب أن تستمر!
- **كيف يتعامل النظام:** لن يقطع النت عن المستخدم! بل سيقوم بتخطي شرط "الدولة أو الـ Tags"، وسيبحث عن *أي* سيرفر متاح (مثلاً سيرفر ألماني)، ويضع المستخدم فيه كـ "سيرفر مؤقت". ويرسل لك إشعاراً طارئاً "تم وضع مستخدم في سيرفر مؤقت لعدم توفر سيرفرات مطابقة". بمجرد أن توفر سيرفرات روسية، سيعيده النظام إليها تلقائياً!

### السيناريو 5: الحذف المفاجئ لسيرفر (Server Deletion Handling) 💥
- **الحدث:** اكتشفت أن سيرفر معين أصبح سيئاً جداً، وقمت بحذفه من لوحة التحكم بضغطة زر. ولكن هناك 50 مستخدم متصلون به!
- **كيف يتعامل النظام:** 
  1. عندما يُحذف السيرفر، يصبح الـ 50 مستخدم فجأة بدون `allocated_server_id`.
  2. المحرك الخلفي (الذي يعمل كل 60 ثانية) سيرصد هذه الفوضى فوراً.
  3. سيضعهم جميعاً في حالة "الدين" (Debt)، ويقوم بتوزيعهم أوتوماتيكياً على السيرفرات الأخرى المتاحة، ويحدث الـ DNS الخاص بهم خلال ثوانٍ. سينقطع عنهم النت لثوانٍ معدودة فقط ثم يعود!

### السيناريو 6: المحرك الخلفي الذي لا ينام (The Background Expiry Checker) 👁️🗨️
- **آلية العمل:** `Thread` (خيط برمجي معزول) يدور كل دقيقة.
- **ماذا يفعل:**
  1. يبحث عن المستخدمين الذين تخطى وقتهم الحالي `subscription_expires_at`.
  2. يقوم بفصلهم (يغير حالتهم لـ `expired`).
  3. **يقوم بحذف الدومين الخاص بهم (DNS)** لمنعهم من الاتصال مجدداً.
  4. محمي داخلياً بـ `try..except` لكل مستخدم. إذا فشل النظام مع مستخدم واحد، لا ينهار المحرك، بل يقفز فوراً لمن يليه.
- **⚠️ ملاحظة معمارية هامة جداً (الحدود التقنية للفصل):**
  نظام GalaxyVPN يعمل كـ "موزع" (Distributor) وليس كمتحكم مباشر بنواة الـ Xray في سيرفراتك. عندما ينتهي وقت المستخدم وتُحذف الدومينات:
  - لن ينقطع الاتصال *فوراً في نفس الثانية* إذا كان المستخدم يشاهد فيديو (لأن اتصال TCP المفتوح لا ينقطع إلا بإغلاقه).
  - الكاش (Cache) الخاص بشركات الاتصالات للـ DNS قد يجعل الدومين يعمل لدقائق إضافية.
  - للفصل الفوري والقطعي (Kick Out)، يجب أن يتم ربط النظام لاحقاً بواجهة برمجة التطبيقات (API) الخاصة بلوحات التحكم (مثل X-UI / Marzban) لحذف الـ UUID من السيرفر نفسه. هذا هو الحل الجذري الوحيد لقطع الاتصال في نفس الثانية!

### السيناريو 7: نظام الاشتراكات المتكامل (Full JSON Config) وحل مشاكل الـ DNS 🌐
- **الحدث:** المستخدم يضع الرابط في تطبيق `v2rayNG` أو `Hiddify`.
- **كيف يتعامل النظام:**
  1. في السابق كان يرسل رابط `vless://`، لكنه كان يتسبب في قطع الإنترنت بسبب غياب إعدادات الـ DNS.
  2. الآن، يقوم النظام باستدعاء محرك `generate_full_config` الذي يبني **كونفيج Xray متكامل (Full JSON)**.
  3. هذا الكونفيج يحتوي على خوادم DNS (Cloudflare 1.1.1.1 و Google 8.8.8.8)، بالإضافة إلى قواعد التوجيه (Routing Rules) ومنافذ الـ `inbounds` (SOCKS/HTTP).
  4. يقوم بتشفيره بصيغة `Base64` ويرسله للمستخدم، ليتعرف عليه التطبيق فوراً ويعمل الإنترنت بكفاءة خيالية بدون أي تدخل يدوي.
  5. **نظام الـ Fallback (الطوارئ):** لو فشل توليد الـ JSON، يرجع النظام آلياً لإرسال رابط `vless://` عادي، ولو فشل يعيد توجيهه بـ IP السيرفر مباشرة.

### السيناريو 8: تكتيك "التسميم الموقوت" (Subscription Poisoning) لقطع الاتصال ☠️
- **المشكلة:** بما أن النظام لا يتصل بـ API السيرفر الأصلي، لا يمكنه فصل اتصال نشط (TCP) حتى لو انتهى وقت المستخدم وحُذف الدومين.
- **الحل الذكي والعبقري:**
  1. يقوم النظام بإرسال أمر خفي `profile-update-interval: 1` في ترويسات (Headers) الاشتراك ليجبر التطبيق (Hiddify/v2rayNG) على التحديث الآلي في الخلفية.
  2. عندما ينتهي وقت المستخدم وتصبح حالته `expired`، يرد النظام بـ "سيرفر وهمي" (Fake Server) يحمل الـ IP الميت `127.0.0.1` ورسالة انتهاء الصلاحية.
  3. التطبيق سيقوم آلياً بمسح السيرفر الحقيقي، واستبداله بالسيرفر الوهمي.
  4. **النتيجة:** ينقطع الإنترنت عن هاتف المستخدم فوراً بطريقة غير قابلة للالتفاف، ودون الحاجة لأي تواصل مع السيرفر الأصلي!

### السيناريو 9: الحماية والأمان العالي (Security Protocols) 🛡️
- **منع اختراق الجلسات (CSRF/XSS/MitM):** ملفات الكوكيز تحمل صلاحيات `SameSite=Lax` و `HttpOnly`، وفي بيئة الإنتاج يتم تفعيل `secure=True` إجبارياً لمنع اعتراض الجلسة عبر الواي فاي.
- **التشفير القوي (Cryptography):** النظام يولد تلقائياً مفتاح `SECRET_KEY` عشوائي مشفر وقوي جداً (32 bytes) لتأمين جلسات التطبيق وتواقيعه.
- **تشفير روابط الاشتراك (Anti-Theft Subscriptions):** الروابط لا تعتمد على الـ `user_id` المجرد (لأنه قد يُسرق)، بل تستخدم توقيعات رقمية مشفرة (HMAC Signatures). لو تم التلاعب بحرف واحد في رابط الاشتراك، يتم حظره فوراً، مما يمنع سرقة الـ VPN Configs.
- **منع استنزاف الخادم (DoS):**
  - لا يمكن للمستخدم رفع مليون إيصال الدفع. زر الرفع يختفي بمجرد رفع الإيصال الأول.
  - عندما تضغط "رفض" لإيصال مزيف، النظام **يحذف الصورة نهائياً من القرص الصلب (Hard Disk)** ولا يتركها تتراكم لتملأ المساحة.
- **وضع الإنتاج الآمن:** إيقاف `Debug=True` لمنع القراصنة من قراءة الكود المصدري عند حدوث خطأ.

### السيناريو 10: تعدد اللغات الديناميكي (Dynamic i18n) 🌍
- **الحدث:** المستخدم يضغط على تغيير اللغة إلى "الروسية".
- **كيف يتعامل النظام:** 
  1. يرسل النظام طلب إلى `/set_language/ru`.
  2. يزرع ملف `Cookie` صالح لسنة كاملة.
  3. يقوم بقلب الـ `HTML` والـ `CSS` (من اليمين لليسار أو العكس عبر `dir="rtl"` / `ltr`).
  4. يتم حقن قاموس `TRANSLATIONS` داخل القوالب بحيث كل كلمة يتم سحبها من القاموس المناسب فوراً بدون إعادة تحميل قاعدة البيانات.

---

## 🛠️ 4. كتيّب الإدارة (The Admin Playbook)
كيف تستخدم أنت (الآدمن) كل هذا؟
1. **رفع السيرفرات دفعة واحدة:** يمكنك تحديد 50 ملف `JSON` دفعة واحدة في لوحة السيرفرات. النظام سيسحب أسماءها من اسم الملف (`Server1.json` يصبح Server1)، وسيستخرج الـ IP، ويرتبها لك بلحظة.
2. **الديون:** راقب دائماً صفحة "إحصائيات الديون". إذا رأيت مستخدمين هناك، هذا يعني أنك بحاجة لإضافة سيرفرات جديدة قريباً لتغطي فتراتهم.
3. **الإشعارات:** الجرس الأحمر يخبرك بأي حركة (تسجيل جديد، دفع، نفاذ سيرفرات). تأكد من ضغط "تحديد الكل كمقروء" بعد الانتهاء.

---
**الخلاصة:**
هذا النظام ليس مجرد "سكربت VPN"، بل هو أشبه بموظف ذكاء اصطناعي يعمل 24/7. يُصلح أخطاءه، يُنظم عملائه، ينقلهم بين السيرفرات بسلاسة، يحمي نفسه من الاختراق، ويوفر لك كل الإحصائيات التي تحتاجها كمدير نظام محترف! 🎩✨
